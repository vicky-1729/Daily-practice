apiVersion: v1
kind: Service
metadata:
  name: nginx-np
spec:
   # nodeport
  type: NodePort

  selector:
   # labels belongs to pod
    purpose: service-np-demo
    project: roboshop
    app: backend
  ports:
    - protocol: TCP
      port: 80 # service
      targetPort: 80 # continer port

---
# pod defination

apiVersion: v1
kind: Pod
metadata:
  name: nginx-np
  labels:
    purpose: service-np-demo
    project: roboshop
    app: backend

spec:
  containers:
  - name: nginx
    image: nginx
